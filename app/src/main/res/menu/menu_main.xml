<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/action_menu"
        android:orderInCategory="100"
        android:icon="@drawable/ic_menu_2x"
        android:title="@string/action_settings"
        app:showAsAction="always|withText">
        <menu>
            <item
                android:id="@+id/action_camera"
                android:title="Take BusinessCard"
                app:showAsAction="always|withText"/>
            <item
                android:id="@+id/action_album"
                android:title="Load BusinessCard"
                app:showAsAction="always|withText"/>
            <item
                android:id="@+id/action_myinfo"
                android:title="My Information"
                app:showAsAction="always|withText"/>
            <item
                android:id="@+id/action_mailcheck"
                android:title="Email Check"
                app:showAsAction="always|withText"/>
        </menu>
    </item>
</menu>
